package com.codefights

/** CodeFights Challenge `almostPalindrome`
  *
  * @author froth30
  * @since  Dec 31, 2017
  */
object almostPalindrome {

  /** Determines whether or not a palindrome can be obtained by swapping at most two pairs of
    * characters in the given string.
    *
    * @example  <li>For `s = "aaabaaa"`,  the output should be `almostPalindrome(s) = true`;
    *           <li>For `s = "abcbcdf"`,  the output should be `almostPalindrome(s) = false`;
    *           <li>For `s = "acac"`,     the output should be `almostPalindrome(s) = true`;
                <li>For `s = "babababa"`, the output should be `almostPalindrome(s) = true`.
    *           </li>
    * @param s  A non-empty string consisting of lowercase alphabetic characters
    *           <ul>''Guaranteed constraints:'' &#9 `1 ≤ s.length ≤ 10`^`5`^
    * @return   `true` if `s` is a palindrome or can be made a palindrome by swapping at most two
    *           pairs of characters; `false` otherwise.
    */
  def almostPalindrome(s: String): Boolean = {
    ???
  }

}
